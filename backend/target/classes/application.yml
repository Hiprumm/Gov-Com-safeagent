server:
  port: 8000

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/postgres?currentSchema=public&useSSL=false&serverTimezone=Asia/Shanghai
    username: postgres
    password: '050303'
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: create
    show-sql: true
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    properties:
      hibernate:
        format_sql: true

logging:
  level:
    com.xiongan: DEBUG
    org.springframework.web: INFO