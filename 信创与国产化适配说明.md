# 信创与国产化适配说明

> SafeAgent 政企大模型智能体安全平台 · 信创/国产化适配说明
> 版本：v1.0（随平台 5.0 发布）

## 1. 适配总览

| 维度 | 适配状态 | 说明 |
|---|---|---|
| 国产大模型（私有化/在线） | ✅ 已支持 | 平台通过 OpenAI 兼容端点接入，支持 DeepSeek、千问（Qwen）、GLM-4 等私有化部署或在线 API |
| 本地模型（Ollama/vLLM） | ✅ 已支持 | 数据不出域：推理完全在内网进行 |
| 国产数据库（达梦 DM8 / 人大金仓 KingbaseES / 神通） | ⚠️ 适配策略已明确 | 平台存储层基于 PostgreSQL 方言编写，需按 §4 迁移策略适配 |
| 国产操作系统（麒麟 / 统信 UOS / 欧拉 openEuler） | ⚠️ 待真机验证 | 纯 Python + 标准依赖，无平台绑定代码；验证清单见 §5 |
| ARM 架构（鲲鹏/飞腾/麒麟 处理器） | ⚠️ 待真机验证 | 依赖（langchain/httpx 等）均有 ARM 版本，建议在真实信创环境跑验证清单 |

## 2. 国产大模型私有化接入步骤

平台「系统状态 → 模型接入配置」提供**国产模型预设**，一键填充接入参数（API Key 手填）：

| 预设 | Base URL | Model | 适用场景 |
|---|---|---|---|
| DeepSeek 官方 API | `https://api.deepseek.com/v1` | `deepseek-chat` | 在线调用 |
| 千问 Qwen2.5 私有化 | `http://<内网地址>/v1` | `qwen2.5-72b-instruct` | vLLM 私有化，数据不出域 |
| GLM-4 私有化 | `https://open.bigmodel.cn/api/paas/v4` | `glm-4` | 智谱网关 |
| Ollama 本地部署 | `http://localhost:11434/v1` | `qwen2.5:7b` | 单机/边缘，完全离线 |

接入步骤：
1. 管理员登录 → 系统状态 → 模型接入配置
2. 选择对应国产模型预设（或手动填写 Base URL / Model）
3. 填入该模型的 API Key（私有化部署一般为网关下发的 Key，Ollama 本地可留空）
4. 点击「主模型连通测试」验证 → 「保存并热生效」
5. 可选：配置「备用模型」实现多供应商容灾（见 §3）

平台内部统一按 OpenAI `chat/completions` 协议对接，保存即热生效，无需重启服务。智能问答、安全检测 LLM 分类层、文档起草均复用同一接入配置。

## 3. 多供应商容灾（配合国产化双路部署）

- 主模型 + 备用模型可配置为**不同厂商**（如主：GLM 私有化，备：千问私有化）；
- 主模型故障/超时 → 自动切换备用模型，保证业务不中断；
- 主模型连续失败 3 次 → 进入 60s 冷却期（期间直走备用），避免"每次先撞主模型超时"拖慢整体延迟；
- 两级模型均不可用 → 对话主链回落规则化应答、安全检测回落本地启发式（仍保持防御能力）；
- 容灾切换次数可在「运维监控」大盘实时观测。

## 4. 数据不出域架构说明

**关键设计**：推理链路中除大模型调用外，平台全部组件（检测、审计、治理、知识库、合规）均在本机/内网运行。

- **模型本地部署时**（Ollama/vLLM 私有化）：输入文本、工具执行结果、审计日志全程不出内网；
- **模型在线调用时**：仅把「当前用户输入文本」发送至模型服务，且：
  - 请求体不含密钥/密码等敏感字段（密钥仅存在于服务端环境变量/加密存储）；
  - 审计日志对个人信息做脱敏存储（PIPL 台账，手机号/证件号等按规则打码）；
  - 知识库文档只做本地检索，不随模型调用外发；
- 平台无任何遥测/回传通道（无第三方统计 SDK）。

## 5. 国产数据库适配策略（达梦 / 人大金仓）

平台存储层（`storage.py` / `postgres_storage.py`）基于 PostgreSQL 方言编写。国产库迁移策略：

| 步骤 | 动作 | 说明 |
|---|---|---|
| 1 | 启用 PostgreSQL 兼容模式 | 达梦、人大金仓均提供 Oracle/PG 兼容模式；建表 SQL 与类型映射基本直接可用 |
| 2 | 替换 JDBC/驱动 | 平台 Python 侧使用 `psycopg2`，国产库建议经网关（如 sharding-proxy 或厂商 PG 兼容接口）接入 |
| 3 | SQL 方言核对 | 平台 SQL 均为标准 DML/DDL + 少量 PG 特性（`ON CONFLICT`、`RETURNING`、`ARRAY`、`JSONB`），适配时替换为国产库等价写法（清单：见 `postgres_storage.py` 中的 `_SCHEMA` 与 `_UPSERT` 语句） |
| 4 | 事务与锁 | 保持 READ COMMITTED + 行锁语义；国产库默认隔离级别需核对 |
| 5 | 全量回归 | 运行 `test_all_apis.py` 全量接口回归 |

> ⚠️ 说明：以上为适配策略与验证清单，实际国产库（达梦/人大金仓）未在当前开发环境实测，正式上线前需在目标环境按本清单执行迁移验证。

## 6. 国产操作系统 / ARM 验证清单

真机（麒麟 V10 / UOS / openEuler，x86_64 或 ARM64）部署验证项：

- [ ] Python 3.10+ 安装与虚拟环境（venv）
- [ ] `pip install -r requirements.txt`（含 langchain、httpx、psycopg2）
- [ ] 后端启动 + `/api/health` 200
- [ ] 前端 `npm run build` + nginx 静态托管 + 反向代理
- [ ] 登录 / 智能问答 / 安全检测全链路
- [ ] 本地 Ollama 模型接入 + 数据不出域验证
- [ ] 审计 WORM 触发器（`audit_protection`）在目标库生效
- [ ] 应急联动 / 熔断 / IP 封锁在目标库正确读写（跨进程一致）

## 7. 结论

平台在**模型层已具备完整的国产化适配能力**（OpenAI 兼容 + 预设 + 私有化部署 + 容灾），
存储与操作系统层给出明确适配策略与验证清单，可在目标信创环境按清单完成迁移验收。
