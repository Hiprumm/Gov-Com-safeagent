# 部署与安全加固指南（生产环境）

> 面向真实环境部署的必读清单。演示/评审环境用默认配置即可；**生产上线前请逐项核对本文第 2、3 节**。
>
> 适用版本：v5.4 及以后（含统一鉴权中间件、审计 WORM、账号与权限体系）。

---

## 1. 部署拓扑建议

```
客户端 / 浏览器
      │  HTTPS（TLS 在反向代理终止）
      ▼
┌──────────────────────────────────────────────┐
│ 反向代理（Nginx / 单位网关）                    │
│  · TLS 终止、限流、IP 白名单                    │
│  · 静态托管前端 dist/，/api /ai /ws 转发后端     │
│  · 生产建议限制 /docs 访问（仅内网运维可见）      │
└───────────────┬──────────────────────────────┘
                ▼
        AI Service（FastAPI :8080）  ← 统一鉴权 / 检测 / 治理 / 审计
                │
      ┌─────────┴──────────┐
      ▼                    ▼
 PostgreSQL（生产推荐）    外部依赖：LLM（智谱或内网 OpenAI 兼容）、
 或 SQLite（单机/演示）     TSA（可选）、SIEM 采集端（可选）
```

- 前端为纯静态产物（`npm run build` 输出 `frontend/dist`），由反向代理托管；
- **WebSocket `/ws/events` 需允许升级**（Nginx 需 `Upgrade`/`Connection` 头透传）；
- 反向代理须透传 `X-Auth-Token`、`X-API-Key` 与真实来源 IP（审计日志记录来源 IP）。

---

## 2. 上线前必做（安全基线）

| # | 项目 | 说明 |
|---|------|------|
| 1 | **配置 `AUTH_JWT_SECRET`** | 未配置时系统会生成并持久化到 `data/jwt_secret.key`（重启不再掉登录态）；**多实例部署必须显式配置同一个密钥**，否则各副本令牌互不认可 |
| 2 | **修改/停用演示账号** | `admin/operator/auditor/user` 默认口令为 `admin123`。后端启动会显式告警"仍在使用默认口令"；生产请改密或停用（后台「用户与组织管理」） |
| 3 | **启用服务间鉴权** | 设 `AUTH_ENABLED=true` + `AUTH_API_KEY=<强随机串>`：此后**所有** HTTP 接口都需 `X-API-Key`（SDK / 网关调用方必须带上）；浏览器端走登录令牌 |
| 4 | **收敛 CORS** | `ALLOWED_ORIGINS` 改为前端实际域名（禁止 `*`，且不得与 `allow_credentials` 同时为 `*`） |
| 5 | **启用限流** | `RATE_LIMIT_ENABLED=true`，按容量设置 `RATE_LIMIT_REQUESTS/WINDOW` |
| 6 | **后端存储选型** | 生产推荐 `STORAGE_BACKEND=postgres` + `POSTGRES_*`；SQLite 仅适合单机/演示 |
| 7 | **数据目录权限** | `ai_service/data/` 收紧为服务账号可读写（含 `*.key`、`*.db`、`jwt_secret.key`、审计归档） |
| 8 | **密钥备份** | 备份 `data/jwt_secret.key`、`data/graded_hmac.key`、`data/zkp_proving.key`——**丢失将导致历史审计签名无法验签** |
| 9 | **审计留存** | 通过 `AUDIT_RETENTION_DAYS`（30/90/180）或「策略配置」设定；系统采用"先归档 JSONL 再清理"，归档目录需纳入备份 |
| 10 | **敏感字段加密密钥** | 配置 `AUTH_MFA_SECRET_KEY`（多实例一致）并备份；未配置时使用 `data/mfa_key.key`（自动生成，0600） |
| 11 | **启用 SSO 时的加固** | 同时配置 `AUTH_SSO_SHARED_SECRET` 与 `AUTH_SSO_ALLOWED_IPS`；网关需按 `X-SSO-Signature = HMAC-SHA256(secret, "<受信头名>:<用户名>")` 注入签名 |
| 12 | **可选：TSA / SIEM** | 「审计追溯」页配置可信时间戳服务地址与 SIEM 外发地址（JSON/CEF） |

---

## 3. 鉴权模型（部署与排障必读）

系统采用**统一鉴权中间件 + 权限点**（`ai_service/main.py` 的 `_PROTECTED_ROUTES` 表集中登记，杜绝逐端点漏写）：

| 类别 | 端点 | 要求 |
|------|------|------|
| **公开（服务间接入面）** | 检测类：`detect_single` / `detect_input` / `detect_file` / `tool_risk` / `operation_guard/check` / `permission/check` / `plugin_scan` / `mcp_scan` / `skill_scan` / `kb/*` / `eval_calc` / `kb/search`；`/api/auth/*`；`/api/health` | 默认放行；启用 `AUTH_ENABLED+AUTH_API_KEY` 后全局需 `X-API-Key` |
| **需登录（任意角色）** | 会话管理：`agent/new_session` `clear_session` `delete_session` `rename_session` `recall_messages` | 登录令牌 |
| **登录或服务密钥** | 智能体执行：`agent/chat` `agent/run` `agent/file_upload` | 登录令牌 或 `X-API-Key` |
| **权限点** | `PUT /api/security/policy` → `policy.manage`；`tool_management/toggle` → `tools.manage`；`runtime/terminate` → `runtime.terminate`；`operation_guard/clear` `session_risk/clear` `cross_source/clear` `kb/rebuild` → `system.maintain`；`optimization/*` `bypass_test` `bypass_*_batch_test` `pssu/assess` `scenarios/*` `replay/*` → `security.scan`；审计治理（WORM/锚点/TSA/外发/归档/留存）与后台用户管理 → 管理员；审批查看/批准 → `approval.view`/`approval.approve`；审计导出 → `audit.export` | 登录令牌 + 权限点 |

**角色→权限**（单一事实来源 `security/permission_engine.py`）：

| 角色 | 关键权限 |
|------|---------|
| admin | 全部（含 `admin.manage` / `policy.manage` / `system.maintain` / `tools.manage` / `runtime.terminate`） |
| operator | 检测/扫描、安全分析（`security.scan`）、工具管控（`tools.manage`）、运行时熔断（`runtime.terminate`）、审批（中低危） |
| auditor | 审计查看与导出（`audit.export`）、审批查看 |
| manager | 本部门数据 + 审批（中低危） |
| user | 智能问答、风险看板、审计查看（仅本人数据） |

**身份贯穿**：反向代理/网关透传 `X-Auth-Token` → 后端由令牌还原真实账号与部门 → 检测、审批、审计均落到具体人（审计按 `user_id → 部门` 反查做数据收敛）。

---

## 4. 已知限制（请评估是否影响你的部署）

| 限制 | 影响 | 建议 |
|------|------|------|
| **进程内存态**：令牌撤销黑名单、登录失败计数、会话锁、能力令牌解锁均在单进程内存 | 多副本部署下各副本状态不一致；重启丢失（审计与审批单已入库，不受影响） | 多副本请开**会话粘滞**，或将其迁移到 Redis/共享库（需二次开发） |
| **审计归档目录为本地路径** | 多副本各自归档、不共享 | 挂载共享存储，或改为集中归档服务 |
| ~~TOTP 密钥明文入库~~ | **已修复**（v5.5）：TOTP 密钥以 Fernet 加密入库（密文前缀 `enc:v1:`），密钥持久化于 `data/mfa_key.key`（权限 0600）或由 `AUTH_MFA_SECRET_KEY` 指定；历史明文数据自动兼容并在下次写入时升级 | 多实例请显式配置**同一个** `AUTH_MFA_SECRET_KEY`；该密钥须纳入备份，否则已绑定用户的 MFA 无法解密 |
| ~~SSO 受信网关头可被伪造~~ | **已加固**（v5.5）：配置 `AUTH_SSO_SHARED_SECRET` 后强制校验 `X-SSO-Signature`（HMAC-SHA256），并支持 `AUTH_SSO_ALLOWED_IPS` 来源白名单；两者都未配置时启动会显式告警 | 生产请**同时**配置共享密钥与 IP 白名单；网关按下述格式注入签名 |
| ~~PostgreSQL 后端未验证~~ | **已验证**（v5.5）：模块级集成测试通过；整机等效回归（后端跑在 PG 上）核心 API 19/19、功能与安全 134/136（2 项为"新库缺 `mgr_biz` 等组织演示账号"，跑 `seed_org_demo.py` 即可）；PG 版 WORM 触发器实测拒绝 UPDATE / DELETE | 上线前仍建议在目标 PG 版本上复跑 `cd ai_service && python test_postgres_backend.py` |
| **检测依赖外部 LLM** | 内网无外网时语义检测能力下降 | 在「系统状态-模型接入」配置内网 OpenAI 兼容端点；未配置时规则层仍可用 |
| **`/docs` 默认开放** | 暴露接口清单 | 反向代理层限制为内网/运维可见 |

---

## 5. 上线检查清单

- [ ] `.env` 已配 `AUTH_JWT_SECRET`（多实例同一值）
- [ ] 演示账号已改密/停用（启动日志无默认口令告警）
- [ ] `AUTH_ENABLED=true` + `AUTH_API_KEY` 已配置，SDK/网关已带 `X-API-Key`
- [ ] `ALLOWED_ORIGINS` 已收敛为实际域名
- [ ] `RATE_LIMIT_ENABLED=true`
- [ ] `STORAGE_BACKEND` 已按生产选型并完成 `test_postgres_backend.py` 验证
- [ ] `data/` 与 `*.key` 权限已收紧，且已纳入备份
- [ ] 审计留存天数已确认，归档目录已纳入备份
- [ ] 反向代理已终止 TLS、限制 `/docs`、透传 `X-Auth-Token`/`X-API-Key`/来源 IP
- [ ] WebSocket 升级已放通（`/ws/events`）
- [ ] 全量回归通过：`test_all_apis.py`(19) / `test_new_features_qa.py`(135) / `test_approval_flow.py`(10) / `test_sdk_gateway.py`(32) / `test_supply_chain_apis.py`(10) / `test_innovation_apis.py`(14) / `test_poison_rules.py`(33) / `test_p1|p3_weak_spot.py`
- [ ] 已验证审计链验签通过、WORM 保护生效（「审计追溯 → 审计保护」显示已安装）

---

## 6. 常见运维问题

| 现象 | 原因与处理 |
|------|-----------|
| 重启后所有会话掉登录 | 未配 `AUTH_JWT_SECRET` 且 `data/jwt_secret.key` 丢失/被清理 |
| 接口返回 401 | 未登录或令牌过期；服务间调用未带 `X-API-Key`。前端会自动回到登录页 |
| 接口返回 403 | 已登录但缺权限点，按第 3 节核对角色权限 |
| 验签失败 | 对应日志在签名后被改动，或数据被外部工具直改。这正是审计链与 WORM 的价值；检查时间线排查 |
| 审计表无法 UPDATE/DELETE | 设计如此（WORM）。需要清理请走「审计追溯 → 执行留存归档」（受控维护窗口） |
