# 前端图片编辑功能修复

## 问题描述

用户上传图片后，点击编辑按钮时，对话框中显示的是：
```
[图片上传] 69afc7260255ddb9555b8395859752ae.png解释一下有什么意思
```

而不是显示实际的图片预览。

## 根本原因

编辑图片消息时，`editMessage()` 函数直接将 `message.content`（存储为 `[图片上传] ${fileName}` 的文本）设到输入框中，而没有显示实际的图片预览。

## 修复方案

### 1. 添加图片预览ref
在脚本中添加 `editingMessageImageUrl` 用于存储编辑中的图片URL：
```typescript
const editingMessageImageUrl = ref<string | null>(null)
```

### 2. 改进 `editMessage()` 函数
根据消息类型进行差异化处理：
- **图片消息**：清空输入框，存储图片URL到 `editingMessageImageUrl`
- **文件消息**：显示 `[文件上传] ${fileName}` 文本
- **文本消息**：正常显示内容

```typescript
const editMessage = async (message: Message) => {
  if (message.type === 'image' && message.imageUrl) {
    inputMessage.value = ''
    editingMessageImageUrl.value = message.imageUrl
  } else if (message.type === 'file') {
    inputMessage.value = `[文件上传] ${message.fileName}`
  } else {
    inputMessage.value = message.content
  }
  editingMessageId.value = message.id
  await recallMessage(message, true)
}
```

### 3. 更新 `sendEditedMessage()` 函数
添加对编辑图片的支持：
- 如果编辑的是图片消息，调用 `/ai/agent/file_upload` 重新上传
- 否则走文本编辑流程

### 4. 模板中添加图片预览
在编辑状态提示下方显示：
```vue
<!-- 编辑图片预览 -->
<div v-if="editingMessageImageUrl" class="mb-2 relative inline-block">
  <img :src="editingMessageImageUrl" alt="编辑中的图片" class="max-w-xs rounded-lg border-2 border-accent/30" />
  <button
    @click="editingMessageImageUrl = null"
    class="absolute -top-2 -right-2 w-6 h-6 bg-red-500 text-white rounded-full flex items-center justify-center hover:bg-red-600 transition-colors"
    title="移除图片"
  >
    <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
      <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M6 18L18 6M6 6l12 12"></path>
    </svg>
  </button>
</div>
```

### 5. 更新 `cancelEdit()` 函数
同时清空输入框和图片预览：
```typescript
const cancelEdit = () => {
  editingMessageId.value = null
  inputMessage.value = ''
  editingMessageImageUrl.value = null
}
```

## 修改文件

- `Gov-Com-safeagent/frontend/src/components/chat/ChatPanel.vue`

## 新功能

1. **图片预览**：编辑图片消息时，现在会显示实际的图片预览而不是文件名
2. **编辑能力**：支持编辑已上传的图片并重新上传进行分析
3. **移除按钮**：图片预览上有移除按钮，点击可清除图片（无需重新开始编辑）

## 使用流程

1. 上传图片 → 点击编辑按钮
2. 现在能看到实际的图片预览（带移除按钮）
3. 可以修改分析请求（输入框），或点击移除改为纯文本请求
4. 点击"重新发送"按钮重新上传和分析

## 验证

✅ 前端构建成功（无TypeScript编译错误）
✅ 支持图片编辑和重新上传
✅ 支持图片移除（改为文本编辑模式）
