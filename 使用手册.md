# 政企大模型智能体安全系统 — 功能使用手册

> **适用版本**：SafeAgent v4.0
> **手册对象**：系统管理员、安全运营、合规审计、业务用户等各类使用人员
> 说明：本手册以**功能使用**为主，从登录到各模块操作全覆盖；部署与安全加固见《部署与安全加固指南.md》，技术架构见 `.trae/documents/TechnicalArchitecture.md`。

---

## 目录

- [1. 系统简介](#1-系统简介)
- [2. 快速开始](#2-快速开始)
- [3. 登录与角色权限](#3-登录与角色权限)
- [4. 工作台导航](#4-工作台导航)
- [5. 智能问答](#5-智能问答)
- [6. 风险看板](#6-风险看板)
- [7. 运行时监控](#7-运行时监控)
- [8. 安全检测](#8-安全检测)
- [9. 红队测试](#9-红队测试)
- [10. 工具管控](#10-工具管控)
- [11. 审批中心](#11-审批中心)
- [12. 审计追溯](#12-审计追溯)
- [13. 策略配置](#13-策略配置)
- [14. 系统状态](#14-系统状态)
- [15. 用户与组织管理](#15-用户与组织管理)
- [16. 评测系统](#16-评测系统)
- [17. 常用接口 / SDK 生态](#17-常用接口--sdk-生态)
- [18. 治理中心](#18-治理中心)
  - [18.1 应急联动](#181-应急联动)
  - [18.2 开放生态管理](#182-开放生态管理)
  - [18.3 PIPL 合规台账](#183-pipl-合规台账)
  - [18.4 合规对标报告](#184-合规对标报告)
- [19. 常见问题(FAQ)](#19-常见问题faq)

---

## 1. 系统简介

面向政企场景的**大模型智能体安全检测与防护系统**，提供从输入安全、工具执行、审批治理到审计溯源的完整纵深防御能力。核心能力包括：

- **多源安全检测**：6 层检测流水线，识别 27 大类攻击（提示注入、间接注入、越狱、命令/SQL 注入、数据投毒、供应链投毒等）
- **智能问答全链路安全执行**：AI 应答过程实时展示「思考中」，危险操作触发人工审批
- **工具执行沙箱**：Docker 隔离执行 + 能力令牌默认拒绝
- **分级签名审计链**：HMAC + Agent 签名 + ZKP + 批量 TSA，防篡改、可溯源
- **账号治理与权限引擎**：RBAC + 数据范围双重鉴权

---

## 2. 快速开始

### 2.1 访问地址

| 环境 | 前端地址 | 后端接口 | API 文档 |
|------|---------|---------|---------|
| 本地开发 | http://localhost:5173 | http://localhost:8080 | http://localhost:8080/docs |
| Docker/生产 | http://localhost | http://localhost:8080 | http://localhost:8080/docs |

> 前端为 Vue 3 SPA，后端为 FastAPI；开发模式下前端自动代理 `/api`、`/ai`、`/ws` 到 `localhost:8080`。

### 2.2 启动（本地开发）

```bash
# 1) 后端
cd ai_service
cp .env.example .env        # 填写 ZHIPU_API_KEY，其余按需
pip install -r requirements.txt
python -m uvicorn main:app --host 0.0.0.0 --port 8080 --reload

# 2) 前端（新终端）
cd frontend
npm install
npm run dev                 # http://localhost:5173
```

### 2.3 启动（Docker / 生产）

```bash
cd deploy
# Windows：start_production.bat
# Linux/Mac：chmod +x start.sh && ./start.sh
```

> 首次启动自动写入演示账号；生产建议在 `ai_service/.env` 配置 `AUTH_JWT_SECRET` 与 PostgreSQL。

---

## 3. 登录与角色权限

### 3.1 登录

首页未登录将跳转登录页，输入**账号 + 密码**即可（支持 MFA/TOTP 二次验证、SSO 免密）。令牌保存在浏览器本地，过程采用 PBKDF2 + JWT 防篡改；连续失败达上限会临时锁定账号。

### 3.2 演示账号（首次启动，密码统一 `admin123`）

| 账号 | 显示名 | 角色 | 说明 |
|------|--------|------|------|
| `admin` | 系统管理员 | admin | 全部模块，管理一切 |
| `operator` | 安全运维员 | operator | 检测 / 审批 / 工具 / 运行时 |
| `auditor` | 合规审计员 | auditor | 看板 / 审计 / 审批查看 |
| `user` | 业务用户 | user | 智能问答 / 风险看板 |

**组织/部门示例账号**（配合数据范围权限演示）：

| 账号 | 角色 | 数据范围 |
|------|------|---------|
| `mgr_sec` | manager | 本部门（信息化与网络安全处）|
| `mgr_biz` | manager | 本部门（政务业务处）|
| `biz_li` / `biz_wang` | user | 仅本人 |

> 角色：`admin` 全量；`operator` 安全运营；`auditor` 合规审计；`manager` 部门负责人；`user` 业务用户。导航按角色自收敛，后端按「模块可见性 + 数据范围 + 审批能力」三重鉴权。

### 3.3 各角色可访问模块速查

| 角色 | 可访问模块 |
|------|-----------|
| admin | 全部（问答/看板/监控/检测/红队/工具/审批/审计/策略/系统/组织）|
| operator | 问答、看板、监控、检测、红队、工具、审批、审计 |
| manager | 问答、看板、审计、审批（部门数据范围）|
| auditor | 问答、看板、审计、审批（只读）|
| user | 问答、看板（仅本人数据）|

---

## 4. 工作台导航

登录后进入主工作台，左侧为功能导航，分三组：

- **日常操作**：智能问答
- **安全运营**：风险看板、运行时监控、安全检测、红队测试、工具管控、审批中心
- **合规审计**：审计追溯
- **系统配置**：策略配置、系统状态

导航依据当前账号权限自动显隐；侧栏可折叠，小屏自动收起。

---

## 5. 智能问答

**功能**：与安全智能体进行对话提问，系统在回答前实时展示「思考路径」，并在必要时调用/审批工具，全程可审计。

### 5.1 发起提问

1. 在底部输入框输入问题，回车或点击发送。
2. 系统立即显示 **「思考中」** 状态，并**流式逐步显现思考过程**（问题分析 → 意图决策 → 规划/信息检索 → 推理 → 答案构建等阶段）。
3. 思考完成后以流式输出最终答复；思考过程与最终回复**分开展示**（思考区为浅色/斜体、可折叠，正文为正常样式）。

![示例：发送问题 → 思考过程 → 最终回复]

### 5.2 消息操作

悬停任一消息，底部出现操作按钮：

| 对象 | 操作 | 说明 |
|------|------|------|
| **用户消息** | 编辑并重发 | 进入编辑态；**编辑不删除历史**，提交后才删除旧消息并覆盖 |
| **用户消息** | 复制 | 复制该消息内容 |
| **用户消息** | 撤回 | 弹出确认框展示影响面（该条及其后 N 条消息、涉及文件/图片），确认后**删除该条及之后全部** |
| **用户消息** | 删除 | 仅删除本条消息 |
| **AI 回复** | 复制 | 复制回复内容 |
| **AI 回复** | 重试 | 撤销并重发对应提问，重新生成新回答（仅支持对文本提问） |

> 撤回时若该条**正在思考中**，会即时中止思考与生成，不残留半截回复；会话历史也已同步删除。

### 5.3 思考过程操作

思考区支持**展开 / 折叠**；每一步可单独查看。思考过程随最终回复一并持久化到会话历史，重新进入会话仍可见，并可作为审计依据。

---

## 6. 风险看板

**功能**：安全态势实时总览，展示检测量、风险分布、攻击类型 TopN、规则命中趋势等可视化指标，支持按权限收敛数据范围。

- 管理员/运营：全量视图
- 审计/管理/业务：仅可见其权限范围内的数据。

---

## 7. 运行时监控

**功能**：实时查看 Agent 执行链路（节点流转、工具调用、耗时、风险阻断），并提供**人工熔断**开关。

- 链路视图：以时间线/节点方式展示当前会话的每一步执行。
- 人工熔断：当某次执行需要人工介入时，可在监控页执行暂停/放行/终止，实现「人在回路」的安全控制。

> 相关事件通过 WebSocket 实时推送，连接不可用时自动回退为按需 HTTP 请求。

---

## 8. 安全检测

**功能**：多源输入检测 + 供应链/插件安全，定位风险并提供风险等级与置信度。

### 8.1 检测模式（子页签）

- **输入检测**：粘贴文本，实时检测攻击意图，返回风险等级（NONE/LOW/MEDIUM/HIGH/CRITICAL）、攻击类型、置信度与命中规则。
- **文件/多源输入**：上传文本或知识库文件，进行投毒与供应链风险扫描。
- **供应链检测**：对 Skill/插件/MCP 描述符等做投毒检测（描述符伪装、异常指令、外部关联等）。

### 8.2 检测流水线（6 层）

| 层 | 名称 | 作用 |
|----|------|------|
| 0 | Unicode 规范化 | NFKC、全角→半角、同形异义字、零宽字符 |
| 0.5 | 高级解码 | Base64、转义、去空格/拼接 SQL 重建 |
| 1 | 规则引擎 | 150+ 正则 + 关键词 |
| 2 | AI 语义检测 | 高中置信度 + 语义模式 |
| 3 | 向量投毒检测 | 知识库基线相似度 + 异常识别 |
| 4 | LLM 语义仲裁 | 智谱 GLM-4-Flash 识别间接攻击（API 不可用自动降级）|

结果为高风险（HIGH/CRITICAL）时，命中工具调用将默认拒绝并转入审批。

---

## 9. 红队测试

**功能**：攻防对抗演练，用变异策略生成绕过测试的对抗样本并批量回归验证防护效果。

- **一键生成变异样本**：内置 10 种变异策略（全角替换、同形异义、零宽字符、空格变体、大小写混淆、编码绕过、分隔符插入、Base64、实体编码、多语言混合）。
- **批量测试**：将样本批量送检，统计检测率、漏报/误报，验证当前策略波纹效果。
- 适合作为上线前的安全回归与演示。

---

## 10. 工具管控

**功能**：工具风险评估 + 能力矩阵，前置约束 Agent 可调用的工具。

- **风险评估**：对工具调用做风险分级，无权限/高风险调用默认拒绝，必要时升级审批。
- **能力矩阵**：按角色展示可用的工具能力（读文件、检索、知识库查询、DB 只读、联网搜索等），形成「最小权限」默认拒绝策略。

---

## 11. 审批中心

**功能**：对需要人工确认的高风险操作进行审批与令牌授予。

### 11.1 审批流程

1. 有权限者（operator/manager/admin）看到**待审批**列表。
2. 审批依据风险等级匹配决策：`LOW` 自动通过；`MEDIUM → manager`；`HIGH → admin`；`CRITICAL → 超管`。
3. 审批人**批准并授予临时令牌**后，对应操作才被放行执行；拒绝则终止该次调用。

### 11.2 待办 / 历史

- **待审批**：处理当前待办（批准/拒绝、授权令牌）。
- **审批历史**：按时间回溯每条审批的决策人、结果、关联操作与审批链，支持搜索筛选。

> 状态通过 WebSocket 实时更新，避免轮询延迟；审批记录自动入审计。

---

## 12. 审计追溯

**功能**：全量操作留痕，防篡改、可责任追溯到具体账号（人）。

- **日志检索**：按时间、角色、操作类型、风险等级、账号/部门筛选。
- **分级签名审计链**：每条记录含分级敏感度、HMAC、Agent 签名与 ZKP 证明，可校验内容未被篡改。
- **WORM / TSA**：防改写存储；批量时间戳（TSA）为时序提供可信锚点。
- **导出**：支持将审计记录导出（用于合规归档）。

---

## 13. 策略配置

**功能**：安全阈值与开关的热配置，无需重启即生效。

可调整项示例：检测阈值、LLM 分类层开关、限流参数、风险等级映射、审批自动通过策略等。配置变更即时作用于后续检测与审批。

---

## 14. 系统状态

**功能**：系统自检与数据维护。

- 查看后端/前端/模型接入/存储等组件运行状态。
- 数据维护：清洗会话、重置演示数据、查看健康状态。

---

## 15. 用户与组织管理

**功能**：组织级账号治理（入口在「用户与组织」管理页，需管理员）。

- 新增员工、按岗位授权（角色/部门/岗位）。
- 启用/停用账号、重置密码。
- 查看部门与数据范围映射（本人/本部门/全量），作为 ABAC 数据范围依据。

---

## 16. 评测系统

**功能**：对检测基线做批量评估并生成报告。

页面入口：首页右上角「评测」（`/evaluate`）。

- 加载内置评测数据集（335 样本：275 攻击 + 60 正常，覆盖 27 类攻击）。
- 逐条送检并输出**准确率 / 精确率 / F1 / 漏报率 / 误报率**与多轮漏报率轨迹。
- 支持导出 Markdown / HTML（浏览器打印为 PDF）。

命令行运行：

```bash
cd ai_service/audit
python run_evaluation.py                    # 跑评测
python generate_report.py                    # 生成 Markdown
python generate_report.py --pdf              # Markdown + HTML(可打印PDF)
```

---

## 17. 常用接口 / SDK 生态

### 17.1 常用接口

| 接口 | 用途 |
|------|------|
| `POST /ai/auth/login` | 登录，换取令牌 |
| `GET /ai/auth/permissions` | 获取当前账号权限/可见模块 |
| `POST /api/agent/chat/stream` | 智能问答流式（SSE，含 `thinking` 思考事件）|
| `GET /api/agent/history` | 会话历史（含思考过程 metadata）|
| `POST /api/agent/recall_preview` | 撤回影响面预览 |
| `POST /api/agent/recall_messages` | 撤回（删该条及之后）|
| `POST /api/agent/delete_message` | 删除单条 |
| `POST /api/security/detect_single` | 单条文本检测 |
| `GET/POST /api/security/**` | 安全检测、文件/供应链检测 |
| `GET /api/approval/*` | 审批待办/历史 |
| `GET /api/audit/**` | 审计日志查询 |
| `GET /api/health` | 健康检查 |

> 完整接口请在运行时访问 `http://localhost:8080/docs`（Swagger UI）交互调试。

### 17.2 SDK / 生态部件

网关（`gateway/`）与 pip SDK（`gov-safeagent-sdk/`）属**独立产品线**，面向"外部 Agent 接入"场景（SDK 嵌入 / 网关中间件 / Sidecar 三种形态），与本主系统的部署形态、端口不同，接入方式见 [docs/外部接入网关与SDK使用说明.md](docs/外部接入网关与SDK使用说明.md)，SDK 内部接口见 `gov-safeagent-sdk/README.md`。

---

## 18. 治理中心

> 面向政企合规与应急的运营模块，含四个子标签：**应急联动**、**开放生态**、**PIPL 合规台账**、**合规对标报告**。需 `system.maintain` 权限（管理员/安全运维），普通用户不可见。
> 对应国家标准落地：等保2.0、GB/T 45654-2025、《生成式人工智能服务管理暂行办法》、《个人信息保护法》、《数据安全法》。

四块功能构成合规从"事前到事后"的闭环：**平时（合规报告）→ 对外（开放生态）→ 过程（PIPL 台账）→ 出事（应急联动）**。

### 18.1 应急联动

**用途**：出事时一键止血 —— 全局熔断、账号/IP 封锁，全程审计留痕，联动通知升级。

**一键全局熔断**
1. 进入「治理中心 → 应急联动」。
2. 「全局熔断」区块点击 **熔断** 开关，弹窗填写原因后确认。
3. 熔断生效特征：指示灯变红并闪烁，状态显示"执行中：所有智能体请求被拦截"；后续智能体请求返回 `403 EMERGENCY_BLOCKED`。
> 处置期间保持"可研读、不可执行"：智能问答、检测、工具执行、审批决策、数据变更等动作一律被拦；但**审计、合规报告、PIPL 台账、开放生态、风险/运行时看板、会话史、通知等只读研判接口仍可查看**（需对应角色权限），便于应急处置期间持续取证与查看态势。IP 封锁则对只读亦拦截（敌对源不可见）。

**封锁 / 解除账号**
1. 在「账号/IP 封锁」的账号输入框填入用户名，填原因 → **封锁**。
2. 该用户后续请求被拦截，其余用户不受影响；排除风险后点该条记录 **解除**。

**封锁 / 解除 IP**
1. 在 IP 输入框填入来源 IP，填原因 → **封锁**。
2. 请求入口中间件拦截该 IP；排除后点 **解除**。

**复位**：熔断解除即恢复；封锁按记录逐条解除。全部操作写入「应急操作记录」，可回溯"谁、何时、对谁、为何"，供审计与复盘。

> 后台核验：`GET /api/emergency/status`，熔断时为 `"global_engaged": true`。

### 18.2 开放生态管理

**用途**：把平台问答/检测能力开放给下属单位或第三方应用，做到**每方独立令牌、独立配额、可限量、可追责**。

**创建调用方并签发 Token**
1. 进入「治理中心 → 开放生态」→ **创建调用方**。
2. 填写：**名称**、**联系信息**、**配额**（总调用次数）、**限流**（每分钟/秒上限）。
3. 提交后返回 `client_id`（如 `cli_xxxx`）和 **Token**（如 `sag_...`）。Token 为 SHA-256 哈希入库，**仅此一次明文显示，请立即复制妥存**，刷新后无法查看。

**下游接入**
```
POST /api/open/chat
Header: X-Client-Token: sag_...
```
系统自动执行：令牌校验 → 限流 → 配额扣减 → 调用打日志。

**治理**
- 列表展示每个调用方：名称、配额、已用、限流、状态。
- **启用/停用**：滥用或到期时停用，其请求立即被拒。
- **删除**：彻底移除（先停用并确认无依赖）。
- **调用日志**：按调用方/时间查看明细，用于追责。
- Token 换新：删除旧调用方 → 新建签发新 Token，旧 Token 即失效。

### 18.3 PIPL 合规台账

**用途**：输入侧个人信息识别与登记（对应《个保法》留痕要求），识别即**自动脱敏**，明文不落库。

**自动识别**：用户输入含手机号/身份证号/银行卡号/邮箱/地址等时，系统自动识别并脱敏后入台账（如 `138*****000`）。

**查看台账**
1. 进入「治理中心 → PIPL 合规台账」。
2. 列表展示：**识别类型、脱敏值、来源（会话）、时间、状态**。
3. 顶部统计卡展示各类型 PII 分布。

**影响评估**
1. 对记录执行 **影响评估**：判断该信息是否必需（能否不采集/少收集）、选定法律依据（同意/必要/法定）、填写结论。
2. 结果写入台账状态，形成处理留痕。

**导出留痕**：台账可导出，作为向监管/合规部门证明"已识别并规范处理个人信息"的证据。

### 18.4 合规对标报告

**用途**：对标等保2.0 / 生成式AI服务 / 数安法，一键生成评分与整改建议，可导出 PDF，用于迎检/备案自测。

**生成报告**
1. 进入「治理中心 → 合规对标报告」→ **生成报告**（自动扫描审计链、词汇库、拒答评估等实时数据）。
2. 展示：**综合评分与等级**（如 `0.875 / B级 基本合规`）、**31 项 GB/T 45654-2025 风险覆盖矩阵**、**等保检查项**（审计要素/6个月留存/防篡改链）、**AIGC 标识**、**拒答率/非拒答率**、**整改建议**。

**导出**：点 **导出** 得到报告文档（HTML/PDF），可直接作备案/测评附件。

**整改闭环**：按建议逐项处理 → 重新生成报告确认对应项 `met` 由 `false` 变 `true`、评分提升 → 重复直至达标再出正式版。

---

## 19. 常见问题(FAQ)

**Q：LLM 语义分类不工作？**
检查 `ai_service/.env` 是否填写 `ZHIPU_API_KEY`；未配置时自动降级为本地启发式规则，不影响基础检测。

**Q：工具沙箱提示 Docker 不可用？**
系统设计了自动降级，Docker 不可用时工具执行切为本地模拟，核心检测功能不受影响。

**Q：撤回/重试有没有影响会话历史？**
撤回会同步删除该条（及之后）的持久化历史；重试会移除旧回复并重新生成。正在思考中的消息被撤回会立即中止，不会残留半截回复。

**Q：页面空白 / WebSocket 断连？**
确认已执行 `npm run build`、Nginx root 路径正确、浏览器控制台无网络错误；WebSocket 不可用时会自动回退为按需 HTTP 请求。

**Q：如何新增自定义检测规则？**
编辑 `ai_service/security/rule_engine.py` 中 `attack_patterns`，按正则 + 权重（0.4~0.5）添加。

**Q：重启后令牌失效？**
未配置 `AUTH_JWT_SECRET` 时重启会重置签名密钥。生产请在 `.env` 显式配置固定密钥，令牌即跨重启有效。

---

*—— 本手册随版本演进持续维护，如有与运行版本不一致处，以实际操作页面为准。*